spring:
  kafka:
    consumer:
      auto-offset-reset: earliest
      group-id: search-service

app:
  booking:
    createtopic: booking-topic
  payment:
    paymenttopic: payment-topic

kafka:
  bootstrapAddress: localhost:9092

