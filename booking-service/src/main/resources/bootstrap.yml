spring:
  application:
    name: booking
  cloud:
    config:
      uri: http://localhost:8191
server:
  port: 0
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  instance:
    instance-id: ${spring.application.name}
    hostname: localhost